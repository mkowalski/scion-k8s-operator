name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Build, lint and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: gofmt
        run: |
          out=$(gofmt -l . | grep -v '^bin/' || true)
          if [ -n "$out" ]; then
            echo "gofmt needs to be run on:" >&2
            echo "$out" >&2
            exit 1
          fi
      - name: go vet
        run: make lint
      - name: Build binaries
        run: make build
      - name: Unit tests
        run: make test
      - name: Envtest suite
        run: |
          make envtest
          KUBEBUILDER_ASSETS=$(bin/setup-envtest use -p path) \
            go test ./internal/operator/controller/ -count=1 -v
      - name: Integration - discovery protocol
        run: ./test/integration/discovery_test.sh

  bundle-check:
    name: OLM bundle up to date
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: bundle-check
        run: make bundle-check

  images:
    name: Image builds
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [agent, operator, registrar]
    steps:
      - uses: actions/checkout@v5
      - name: Build ${{ matrix.image }} image
        run: docker build -f build/Dockerfile.${{ matrix.image }} -t scion-${{ matrix.image }}:ci .

  kind-e2e:
    name: kind e2e (vanilla Kubernetes)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install kind
        run: |
          sudo curl -fsSLo /usr/local/bin/kind \
            https://kind.sigs.k8s.io/dl/v0.31.0/kind-linux-amd64
          sudo chmod +x /usr/local/bin/kind
      - name: Run kind e2e
        run: ./test/e2e/kind/run.sh
